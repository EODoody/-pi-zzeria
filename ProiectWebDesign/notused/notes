<?php
    // Connect to the database
    $servername = "localhost";
    $username = "root";
    $password = "";
    $dbname = "pizzeria3.14";

    $conn = new mysqli($servername, $username, $password, $dbname);
    if ($conn->connect_error) {
        die("Connection failed: " . $conn->connect_error);
    }


    ob_start();

    // Retrieve the image from the database
    $sql = "SELECT Pizza_Image FROM pm_pizza where Pizza_ID = 2";
    $result = $conn->query($sql);
    $row = $result->fetch_assoc();
    
    $image = $row['Pizza_Image'];

    // Set the content type header to the appropriate image type
    header('Content-Type: image/jpeg');

    // Output the image
    echo $image;
    ob_end_flush();

    $conn->close();
?>

THIS WORKS FOR A SINGULAR PICTURE





========



<!-- Black with grey text NAVBAR -->
<nav class="navbar navbar-expand-md bg-dark navbar-dark">
      <div class="container-fluid">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" href="#">Dood &pi;zzeria</a>
          </li>
        <li class="nav-item">
            <a class="nav-link" href="meniu.php">Meniu</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="promotii.php">Promotii</a>
          </li>
          <li class="nav-item">
            <a class="nav-link disabled" href="#">Locatii Dine-in</a>
          </li>
        </ul>
        <!-- Login/signup button -->
        <button class="login-button" onclick="window.location.href='login.html'">Log In</button>
      </div>
    </nav>








    if (isset($_SESSION['user_id']) && $_SESSION['user_id'] != 0)
            echo'<button class="login-button" href="logout.php">Log Out</button>';
         else
            echo'<button class="login-button" href="login.html">Log In</button>';